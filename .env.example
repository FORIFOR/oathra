# Oathra — copy to .env (auto-loaded by the CLI). Never commit .env.

# Brains
OPENAI_API_KEY=
GEMINI_API_KEY=
# OLLAMA_HOST=http://127.0.0.1:11434

# Real phone calls (oathra call)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=
DEEPGRAM_API_KEY=

# Your own phone for `oathra call` smoke tests (E.164). Not required.
OATHRA_TEST_PHONE=
